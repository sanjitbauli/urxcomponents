import PropTypes from 'prop-types';
import React from 'react';
type Props = {
    leftText: string;
    contentClass: string;
};
declare const _default: {
    ({ leftText, contentClass }: Props): React.JSX.Element;
    propTypes: {
        leftText: PropTypes.Requireable<string>;
    };
};
export default _default;
